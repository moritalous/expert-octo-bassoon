# フィード実装メモ

## 対応方針
- RSS だけでなく Atom フィードも考慮して実装・テストする。
- フィードパーサのようなフォーマット依存の処理は、サンプル文字列で再現できるテストを追加する。

## テーマソース方針
- 主ソースは `config/themes.json` の `google_alert_rss`。
- 補助ソースは `supplemental_rss` に明示したものだけを本番収集対象に含める。
- 試験用フィードは本番の朝刊生成に混ぜない。
